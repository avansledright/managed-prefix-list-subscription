# managed-prefix-list-subscription

A Python script to automate updating AWS Managed Prefix Lists.

## Usage:
1. Update the method in which you want to obtain your IPs
2. Pass the variables into the function.